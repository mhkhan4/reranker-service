services:
  reranker-service:
    environment:
      - DEVICES=cuda:0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
