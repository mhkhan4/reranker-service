services:
  reranker-service:
    environment:
      - DEVICES=cuda:0
      - API_KEY=${API_KEY}
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
